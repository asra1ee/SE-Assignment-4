# 区块链基础及应用 Exercise 6 实验报告

**姓名：** 李佳泽、章壹程  
**学号：** 2311904、2313469  
**日期：** 2026年1月4日

---

## 一、实验目的

1. 学习 circom，一个描述算术电路的工具
2. 学习 snarkjs，一种用于生成和验证电路满意度的 zk-SNARKs 工具
3. 探索私有事务（private transactions）的实现
4. 制作一个简化版本的 Tornado Cash 花费电路
5. 生成赎回 Tornado 的有效性证明

---

## 二、实验环境

- 操作系统：Ubuntu 20.04
- Node.js 版本：10.x 或更高
- circom 版本：alex-ozdemir/circom#cs251
- snarkjs 版本：0.1.11
- mocha 测试框架

---

## 三、实验内容及核心代码

### 3.1 了解 circom 基础

通过阅读 `TUTORIAL.md` 和 `circuits/example.circom`，理解了以下关键概念：

- **信号（signals）**：电路中的变量，分为输入信号和中间信号
- **`<--` 运算符**：用于计算信号值的赋值操作
- **`===` 运算符**：用于定义 rank-1 约束
- **`<==` 运算符**：同时执行赋值和约束（`<--` + `===`）

#### 示例电路分析

```circom
template Num2Bits(b) {
    signal input in;
    signal output bits[b];

    // 使用 <-- 进行位分解赋值
    for (var i = 0; i < b; ++i) {
        bits[i] <-- (in >> i) & 1;
    }

    // 约束每个位必须是 0 或 1
    for (var i = 0; i < b; ++i) {
        bits[i] * (1 - bits[i]) === 0;
    }

    // 验证位的和等于输入
    var sum_of_bits = 0;
    for (var i = 0; i < b; ++i) {
        sum_of_bits += (2 ** i) * bits[i];
    }
    sum_of_bits === in;
}
```

### 3.2 SmallOddFactorization 证明生成

使用 `SmallOddFactorization` 电路为 7×17×19=2261 创建证明：

```bash
# 1. 编译电路
circom ./circuits/example.circom -o ./example.json

# 2. 查看电路信息
snarkjs info -c example.json
# 输出：
# Wires: 32
# Constraints: 23
# Private Inputs: 3
# Public Inputs: 1
# Outputs: 0

# 3. 设置 proving key 和 verification key
snarkjs setup -c example.json

# 4. 创建输入文件 input.json
echo '{"product": "2261", "factors": ["7", "17", "19"]}' > input.json

# 5. 计算 witness
snarkjs calculatewitness -c example.json

# 6. 生成证明
snarkjs proof -c example.json

# 7. 验证证明
snarkjs verify
# 输出: OK
```

### 3.3 IfThenElse 电路实现

```circom
template IfThenElse() {
    signal input condition;
    signal input true_value;
    signal input false_value;
    signal output out;

    // 约束 condition 必须是 0 或 1
    condition * (1 - condition) === 0;

    // 使用辅助信号实现条件选择
    // out = condition * true_value + (1 - condition) * false_value
    signal temp;
    temp <== condition * true_value;
    out <== temp + (1 - condition) * false_value;
}
```

**代码说明：**
- 首先约束 `condition` 必须是 0 或 1
- 使用公式 `out = condition * true_value + (1 - condition) * false_value` 实现条件选择
- 当 `condition = 1` 时，`out = true_value`
- 当 `condition = 0` 时，`out = false_value`

### 3.4 SelectiveSwitch 电路实现

```circom
template SelectiveSwitch() {
    signal input in0;
    signal input in1;
    signal input s;
    signal output out0;
    signal output out1;

    // 使用 IfThenElse 实现选择性交换
    component ite0 = IfThenElse();
    component ite1 = IfThenElse();

    // 当 s=0 时保持顺序，当 s=1 时交换
    ite0.condition <== s;
    ite0.true_value <== in1;
    ite0.false_value <== in0;
    out0 <== ite0.out;

    ite1.condition <== s;
    ite1.true_value <== in0;
    ite1.false_value <== in1;
    out1 <== ite1.out;
}
```

**代码说明：**
- 利用两个 `IfThenElse` 组件实现选择性交换
- 当 `s = 0` 时：`out0 = in0`, `out1 = in1`（保持顺序）
- 当 `s = 1` 时：`out0 = in1`, `out1 = in0`（交换顺序）

### 3.5 Spend 电路实现

```circom
template Spend(depth) {
    signal input digest;
    signal input nullifier;
    signal private input nonce;
    signal private input sibling[depth];
    signal private input direction[depth];

    // 计算硬币哈希 coin = H(nullifier, nonce)
    component coinHash = Mimc2();
    coinHash.in0 <== nullifier;
    coinHash.in1 <== nonce;

    // 从叶子节点向上计算 Merkle 路径
    component switches[depth];
    component hashes[depth];
    
    signal pathHashes[depth + 1];
    pathHashes[0] <== coinHash.out;

    for (var i = 0; i < depth; i++) {
        // 根据 direction 决定哈希顺序
        switches[i] = SelectiveSwitch();
        switches[i].in0 <== pathHashes[i];
        switches[i].in1 <== sibling[i];
        switches[i].s <== direction[i];

        // 计算父节点哈希
        hashes[i] = Mimc2();
        hashes[i].in0 <== switches[i].out0;
        hashes[i].in1 <== switches[i].out1;
        pathHashes[i + 1] <== hashes[i].out;
    }

    // 约束计算得到的根哈希等于提供的摘要
    digest === pathHashes[depth];
}
```

**代码说明：**
- 首先计算硬币哈希 `coin = H(nullifier, nonce)`
- 使用 `SelectiveSwitch` 根据 `direction` 决定哈希的输入顺序
- 逐层向上计算 Merkle 路径，最终约束根哈希等于提供的摘要

### 3.6 computeInput 函数实现

```javascript
function computeInput(depth, transcript, nullifier) {
    // 创建稀疏 Merkle 树
    const tree = new SparseMerkleTree(depth);
    
    let targetNonce = null;
    let targetIndex = -1;

    // 遍历所有硬币，添加到树中
    for (let i = 0; i < transcript.length; i++) {
        const entry = transcript[i];
        let coin;
        
        if (entry.length === 1) {
            // 单个值，直接是硬币
            coin = BigInt(entry[0]);
        } else {
            // 两个值：nullifier 和 nonce
            const entryNullifier = entry[0];
            const entryNonce = entry[1];
            coin = mimc2(BigInt(entryNullifier), BigInt(entryNonce));
            
            // 检查是否是目标 nullifier
            if (entryNullifier === nullifier) {
                targetNonce = entryNonce;
                targetIndex = i;
            }
        }
        
        tree.insert(i, coin);
    }

    // 获取 Merkle 路径
    const path = tree.path(targetIndex);
    
    // 构建输出对象
    const result = {
        "digest": tree.digest.toString(),
        "nullifier": nullifier,
        "nonce": targetNonce
    };

    // 添加 sibling 和 direction
    for (let i = 0; i < depth; i++) {
        result[`sibling[${i}]`] = path[i].sibling.toString();
        result[`direction[${i}]`] = path[i].direction ? "1" : "0";
    }

    return result;
}
```

**代码说明：**
- 创建深度为 `depth` 的稀疏 Merkle 树
- 遍历 transcript，将硬币添加到树中
- 找到目标 nullifier 对应的 nonce 和索引
- 获取 Merkle 路径，构建电路输入对象

---

## 四、实验结果

### 4.1 环境配置验证

初始测试结果（大部分测试失败，说明环境配置成功）：

![初始测试](images/test_initial.png)

```
IfThenElse
  1) should give `false_value` when `condition` = 0
  2) should give `true_value` when `condition` = 1
  3) should enforce that s in {0, 1}

SelectiveSwitch
  4) should not switch when s = 0
  5) should switch when s = 1
  6) should enforce that s in {0, 1}

computeInput
  7) transcript0.txt, depth 0, nullifier 1
  8) transcript1.txt, depth 4, nullifier 4
  9) transcript2.txt, depth 25, nullifier 7

Spend
  ✓ witness computable for depth 0
  ✓ witness computable for depth 1
  ✓ witness computable for depth 2
  10) witness not computable for bad input

3 passing (200ms)
10 failing
```

### 4.2 SmallOddFactorization 证明

编译电路并查看信息：

![电路信息](images/circuit_info.png)

```bash
$ circom ./circuits/example.circom -o ./example.json
$ snarkjs info -c example.json
# Wires: 32
# Constraints: 23
# Private Inputs: 3
# Public Inputs: 1
# Outputs: 0
```

生成并验证证明：

![验证通过](images/verify_ok.png)

```bash
$ snarkjs calculatewitness -c example.json
$ snarkjs proof -c example.json
$ snarkjs verify
OK
```

### 4.3 完成所有电路后的测试结果

所有测试通过：

![全部测试通过](images/test_all_pass.png)

```
IfThenElse
  ✓ should give `false_value` when `condition` = 0
  ✓ should give `true_value` when `condition` = 1
  ✓ should enforce that s in {0, 1}

SelectiveSwitch
  ✓ should not switch when s = 0
  ✓ should switch when s = 1
  ✓ should enforce that s in {0, 1}

computeInput
  ✓ transcript0.txt, depth 0, nullifier 1
  ✓ transcript1.txt, depth 4, nullifier 4
  ✓ transcript2.txt, depth 25, nullifier 7

Spend
  ✓ witness computable for depth 0
  ✓ witness computable for depth 1
  ✓ witness computable for depth 2 (806ms)
  ✓ witness not computable for bad input (812ms)

13 passing (2s)
```

### 4.4 深度10的 Spend 证明生成

为 `transcript3.txt` 生成 nullifier "10137284576094" 的证明：

![Spend10验证](images/spend10_verify.png)

```bash
# 生成输入文件
$ node src/compute_spend_inputs.js 10 './test/compute_spend_inputs/transcript3.txt' 10137284576094 -o input.json

# 编译深度为10的 Spend 电路
$ circom ./test/circuits/spend10.circom -o circuit.json

# 设置
$ snarkjs setup

# 计算 witness
$ snarkjs calculatewitness

# 生成证明
$ snarkjs proof

# 验证证明
$ snarkjs verify
OK
```

---

## 五、生成的证明文件

### 5.1 verification_key_factor.json

SmallOddFactorization 电路的验证密钥，用于验证因数分解证明。

### 5.2 proof_factor.json

7×17×19=2261 的零知识证明。

### 5.3 verification_key_spend.json

深度为10的 Spend 电路的验证密钥。

### 5.4 proof_spend.json

nullifier "10137284576094" 存在于 Merkle 树中的零知识证明。

---

## 六、实验总结

### 6.1 关键知识点

1. **R1CS（Rank-1 Constraint System）**：零知识证明的基础，约束形式为 A×B=C，其中 A、B、C 都是信号的线性组合。

2. **circom 运算符**：
   - `<--`：计算赋值，不添加约束
   - `===`：添加 rank-1 约束
   - `<==`：同时赋值和约束

3. **Merkle 树证明**：通过提供路径上的兄弟节点哈希值，可以证明某个叶子节点属于特定的 Merkle 树。

4. **零知识性质**：
   - 证明者可以证明知道某些秘密信息
   - 验证者无法获取任何关于秘密信息的知识
   - 在本实验中，用户可以证明拥有某个硬币，而不透露具体是哪个硬币

### 6.2 实验收获

1. 掌握了 circom 电路描述语言的基本语法和使用方法
2. 理解了 zk-SNARKs 的工作流程：编译→设置→计算witness→生成证明→验证
3. 实现了隐私交易系统的核心组件：条件选择、选择性交换、花费证明
4. 深入理解了 Tornado Cash 类隐私协议的工作原理

### 6.3 注意事项

1. 位运算（如 `&`、`|`、`>>`）只能用于 `<--` 赋值，不能用于 `===` 约束
2. 确保所有约束都是 rank-1 形式（最多一次乘法）
3. 信号的线性组合（常量×信号的和）仍然是线性组合，可以用于约束

---

## 七、参考资料

1. circom 官方文档
2. snarkjs GitHub 仓库
3. Tornado Cash 白皮书
4. 课程提供的 TUTORIAL.md 文件
